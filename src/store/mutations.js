export default {
  setEmail(context, email) {
    context.email = email;
  },
  setName(context, name) {
    context.name = name;
  },
  setPhone(context, phone) {
    context.phone = phone;
  },
  setCity(context, city_id) {
    context.city_id = city_id;
  },
};
